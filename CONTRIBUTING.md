# Contributing to MonadPay

We welcome contributions! Please follow these steps:

1. Fork the repo
2. Create a branch: \git checkout -b feat/your-feature\`n3. Make changes and commit: \git commit -m 'feat: description'\`n4. Push and open a PR against \main\`n
## Code Style
- Use TypeScript strictly (no \ny\ unless necessary)
- Follow existing component patterns
- Use custom hooks for all business logic

